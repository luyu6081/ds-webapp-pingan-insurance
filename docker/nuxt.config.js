import dotenv from 'dotenv'

dotenv.config()

export default {
  mode: 'spa',
  // modules: [
  //   '@nuxtjs/proxy',
  // ],
  // proxy: {
  //   '/graphql': process.env.NUXT_ENV_HTTP_ENDPOINT,
  //   '/auth': process.env.NUXT_ENV_AUTH_HTTP_ENDPOINT,
  // },
}
